# @Messanga11/core

Cross-platform business logic core for Messanga11 applications.

## Installation

```sh
npm install @messanga11/core
```

## Development

```sh
npm install
npm run check
npm run typecheck
npm test
npm run build
```

The npm identifier is lowercase because npm package names are case-sensitive and must not contain uppercase letters.
